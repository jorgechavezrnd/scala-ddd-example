# Define here your desired values for each variable
# And copy this `.env.dist` file to this same directory with the `.env` name
# in order to be used by the `docker-compose.yml`

# DB container variables
CODELYTV_SCALA_API_DB_PORT=3316
